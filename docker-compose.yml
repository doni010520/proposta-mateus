version: '3.8'

services:
  api:
    build: .
    container_name: solar-proposal-api
    ports:
      - "3737:3737"
    volumes:
      - ./outputs:/app/outputs
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
